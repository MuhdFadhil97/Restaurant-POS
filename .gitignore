node_modules/
dist/
build/
.env
.env.local
*.log
.DS_Store
coverage/
backend/uploads/

# License signing keys / issued license files — never commit these.
backend/keys/
*.pem
*.license

# PM2 production process config — contains JWT_SECRET/LICENSE_KEY inline.
backend/ecosystem.config.js

# Fake thermal printer output (backend/scripts/fake-printer.ts)
backend/fake-printer-output/

# Local cloudflared tunnel runtime files
.cloudflared.out
.cloudflared.pid

# Netlify CLI local state
frontend/.netlify/
.netlify/
