VITE_API_URL=http://localhost:4000/api

# Base URL customers' phones can reach to scan the e-Invoice QR / call the API.
# Set this to your till PC's LAN IP (e.g. http://192.168.1.5:5173) since
# "localhost" resolves to the phone itself, not this machine.
VITE_PUBLIC_URL=
